import { BulletedTextArea, MuiBulletedTextArea } from '../index'

describe('BulletedTextArea', () => {
  it('is truthy', () => {
    expect(BulletedTextArea).toBeTruthy()
  })
})

describe('MuiBulletedTextArea', () => {
  it('is truthy', () => {
    expect(MuiBulletedTextArea).toBeTruthy()
  })
})
